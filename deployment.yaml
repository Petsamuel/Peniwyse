---
apiVersion: v1
kind: Namespace
metadata:
  name: dev-cred-ns
  labels:
    name: dev-cred-ns
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: peniwyse-deployment
  namespace: dev-cred-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: peniwyse-service
  template:
    metadata:
      labels:
        app: peniwyse-service
    spec:
      nodeSelector:
        "kubernetes.io/os": linux
      containers:
      - name: peniwyse-service
        image: swr.af-south-1.myhuaweicloud.com/cred-dev/peniwyse-service:15a8d16cacb23ad3b87debb80e5c3c962ad9e3ee
        imagePullPolicy: "IfNotPresent"
        ports:
          - containerPort: 3000
        # envFrom:
        #   - secretRef:
        #       name: peniwyse-secret
        resources:
          requests:
            cpu: 100m
            memory: 156Mi
          limits:
            cpu: 200m
            memory: 212Mi
      imagePullSecrets:
      - name: default-secret
---
apiVersion: v1
kind: Service
metadata:
  name: peniwyse-service
  namespace: dev-cred-ns
spec:
  type: NodePort # Change service type to NodePort
  selector:
    app: peniwyse-service
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
    #nodePort: 30617 # Define a specific nodePort value (replace with desired port number)
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: kubeissuer-peniwyse
  namespace: dev-cred-ns
spec:
  acme:
    # The ACME server URL
    server: https://acme-v02.api.letsencrypt.org/directory
    # Email address used for ACME registration
    email: seun.adegbite@credlanche.com
    # Name of a secret used to store the ACME account private key
    privateKeySecretRef:
      name: kubeissuer-peniwyse
    # Enable the HTTP-01 challenge provider
    solvers:
    - http01:
        ingress:
          class: nginx
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kubecert-peniwyse
  namespace: dev-cred-ns
spec:
  secretName: peniwyse
  issuerRef:
    name: kubeissuer-peniwyse
    kind: ClusterIssuer
  duration: 2160h # 90d
  renewBefore: 360h # 15d
  commonName: peniwyse-dev.digitvant.com
  dnsNames:
  - peniwyse-dev.digitvant.com

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
 name: peniwyse-ingress
 namespace: dev-cred-ns
 annotations:
   #cert-manager.io/cluster-issuer: letsencrypt-staging
   kubernetes.io/ingress.class: nginx
spec:
 ingressClassName: nginx
 tls:
 - hosts:
   - peniwyse-dev.digitvant.com
   #secretName: mp-staging-tls
   secretName: peniwyse
 rules:
 - host: peniwyse-dev.digitvant.com
   http:
     paths:
     - pathType: Prefix
       path: /
       backend:
         service:
           name: peniwyse-service
           port:
             number: 3000